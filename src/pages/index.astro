---
import { fetchArticles } from "@lib/api";
import Blog from "@layouts/blog";
import { Preview } from "@tmp-components/Preview";

const articles = await fetchArticles();
---

<Blog>
	{articles.contents.map((article) => <Preview article={article} />)}
</Blog>
